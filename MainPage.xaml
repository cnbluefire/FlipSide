<Page
    x:Class="FlipSide.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FlipSide"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:control="using:FlipSide.Controls"
    mc:Ignorable="d"
    RequestedTheme="Light"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <control:FlipSide x:Name="_FlipSide" Width="300" Height="200">
            <control:FlipSide.Side1>
                <Grid Width="300" Height="200" Background="#007acc">
                    <Button x:Name="GoTo2" Click="Button_Click" Padding="100,70" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White">翻页</Button>
                </Grid>
            </control:FlipSide.Side1>
            <control:FlipSide.Side2>
                <Grid Width="300" Height="200" Background="#ca5100">
                    <Button x:Name="GoTo1" Click="Button_Click" Padding="100,70" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White">翻页</Button>
                </Grid>
            </control:FlipSide.Side2>
        </control:FlipSide>
    </Grid>
</Page>
